@model IEnumerable<Job_Portal_System.APPLICATION>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_JobSeekerDashboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.applied_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JOB.job_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JOB_SEEKER_PROFILE.gender)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.applied_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JOB.job_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JOB_SEEKER_PROFILE.gender)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.application_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.application_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.application_id })
        </td>
    </tr>
}

</table>
